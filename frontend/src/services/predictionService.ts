import { api } from './api'; export async function analyzePrediction(file:File,cropType:string,notes?:string){const form=new FormData(); form.append('image',file); form.append('crop_type',cropType); if(notes) form.append('notes',notes); const {data}=await api.post('/predictions/analyze',form,{headers:{'Content-Type':'multipart/form-data'}}); return data;} export async function getPredictions(){return (await api.get('/predictions')).data}
