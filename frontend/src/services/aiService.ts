import { api } from './api'; export async function sendChatMessage(message:string){try{return (await api.post('/ai/chat',{message,context_type:'general'})).data}catch{return {response:'AI assistant is running in demo mode because API key is not configured. Please upload a clear leaf image for better advisory output and consult a local agriculture expert for serious crop damage.'}}}
