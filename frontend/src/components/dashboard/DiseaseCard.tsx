import type { Disease } from '../../types'; import { Card } from '../common/Card'; import { SeverityBadge } from '../common/SeverityBadge'; export function DiseaseCard({d}:{d:Disease}){return <Card className="p-5"><div className="flex items-start justify-between gap-3"><div><p className="text-sm text-green-700">{d.cropType}</p><h3 className="mt-1 text-lg font-bold text-slate-950">{d.name}</h3></div><SeverityBadge severity={d.severity}/></div><p className="mt-4 text-sm text-slate-600">{d.symptoms.slice(0,2).join(', ')}</p><div className="mt-4 rounded-xl bg-green-50 p-3 text-sm text-green-800">{d.prevention[0]}</div></Card>}
