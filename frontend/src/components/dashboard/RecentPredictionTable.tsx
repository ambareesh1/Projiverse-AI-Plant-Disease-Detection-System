import { Link } from 'react-router-dom'; import type { Prediction } from '../../types'; import { SeverityBadge } from '../common/SeverityBadge';
export function RecentPredictionTable({items}:{items:Prediction[]}){return <div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full text-left"><thead className="bg-slate-50 text-sm font-semibold text-slate-600"><tr><th className="p-4">Crop</th><th>Disease</th><th>Confidence</th><th>Severity</th><th>Date</th><th>Action</th></tr></thead><tbody>{items.map(p=><tr key={p.id} className="border-b border-slate-100 hover:bg-green-50/40"><td className="p-4">{p.cropType}</td><td>{p.diseaseName}</td><td>{p.confidence}%</td><td><SeverityBadge severity={p.severity}/></td><td>{new Date(p.createdAt).toLocaleDateString()}</td><td><Link className="font-semibold text-green-700" to={`/result/${p.id}`}>View</Link></td></tr>)}</tbody></table></div></div>}
