import { SectionHeader } from '../common/SectionHeader'; import { Card } from '../common/Card'; import { Button } from '../common/Button'; import { Link } from 'react-router-dom';
const items=['Upload plant leaf image', 'Select crop type', 'AI model analyzes image', 'System predicts disease', 'User gets treatment and prevention tips', 'Result is saved in prediction history'];
export function HowItWorksSection(){return <section className="home-section mx-auto max-w-7xl px-4 py-16"><SectionHeader title="How it works" subtitle="From image upload to advisory report in six clear steps." /> <div className="grid gap-4 md:grid-cols-{items.length>3?4:2}">{items.map((item,i)=><Card key={item} className="landing-card p-5 hover:-translate-y-1"><div className="mb-4 flex h-10 w-10 items-center justify-center rounded-xl bg-green-50 font-bold text-green-700">{i+1}</div><p className="font-semibold text-slate-800">{item}</p></Card>)}</div></section>}
