export function ConfidenceScore({value}:{value:number}){const label=value>=90?'High confidence':value>=70?'Good confidence':value>=50?'Moderate confidence':'Low confidence'; const color=value>=90?'bg-green-600':value>=70?'bg-sky-600':value>=50?'bg-amber-500':'bg-red-500'; return <div><div className="mb-2 flex items-center justify-between"><span className="font-semibold text-slate-800">{value.toFixed(1)}%</span><span className="text-sm text-slate-500">{label}</span></div><div className="h-2 rounded-full bg-green-100"><div className={`h-2 rounded-full ${color}`} style={{width:`${value}%`}} /></div></div>}
