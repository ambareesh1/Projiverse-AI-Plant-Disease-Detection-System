import { UploadCloud } from 'lucide-react';
export function UploadBox({onFile,image}:{onFile:(f:File)=>void;image?:string}){return <label className="block cursor-pointer rounded-3xl border-2 border-dashed border-green-300 bg-green-50/50 p-8 text-center transition-all duration-300 hover:border-green-400 hover:bg-green-50"><input type="file" accept="image/jpeg,image/png,image/webp" className="hidden" onChange={e=>e.target.files?.[0]&&onFile(e.target.files[0])}/>{image?<img src={image} className="mx-auto h-56 rounded-2xl object-cover"/>:<><UploadCloud className="mx-auto h-12 w-12 text-green-600"/><p className="mt-4 font-semibold text-slate-800">Drag and drop a clear leaf image or click to browse</p><p className="mt-2 text-sm text-slate-500">JPG, PNG, WEBP up to 5MB</p></>}</label>}
