import { useParams } from 'react-router-dom'; import { Button } from '../components/common/Button'; import { predictions } from '../data/mockData';
export function ReportPage(){const {id}=useParams(); const p=predictions.find(x=>String(x.id)===id)||predictions[0]; const block=(t:string,a:string[])=><section className="mt-5"><h2 className="border-l-4 border-green-600 pl-3 text-lg font-bold">{t}</h2><ul className="mt-2 list-disc pl-6 text-slate-700">{a.map(x=><li key={x}>{x}</li>)}</ul></section>; return <main className="bg-slate-100 p-4"><div className="no-print mx-auto mb-4 max-w-4xl"><Button onClick={()=>window.print()}>Print / Save as PDF</Button></div><article className="mx-auto max-w-4xl bg-white p-10 shadow"><h1 className="text-3xl font-bold text-green-700">AI Plant Disease Detection System Report</h1><p className="mt-2 text-slate-500">Prediction ID: {p.id} • Date: {new Date(p.createdAt).toLocaleDateString()}</p><img src={p.imageUrl} className="mt-6 h-56 rounded-2xl object-cover"/><div className="mt-6 grid grid-cols-2 gap-4"><p><b>Crop:</b> {p.cropType}</p><p><b>Disease:</b> {p.diseaseName}</p><p><b>Confidence:</b> {p.confidence}%</p><p><b>Severity:</b> {p.severity}</p></div>{block('Possible Causes',p.possibleCauses)}{block('Treatment Suggestions',p.treatmentSuggestions)}{block('Prevention Tips',p.preventionTips)}<section className="mt-5"><h2 className="border-l-4 border-green-600 pl-3 text-lg font-bold">AI Explanation</h2><p className="mt-2 text-slate-700">{p.aiExplanation}</p></section><p className="mt-8 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-800">This system provides advisory and educational information only. It does not guarantee diagnosis. For serious crop damage, consult a local agriculture expert.</p></article></main>}
