# Model Workflow

The MVP uses a mock model by crop type. The real workflow is image preprocessing, resize/normalize, CNN inference, label decoding, confidence calculation, severity mapping, and explanation generation.
