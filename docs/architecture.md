# Architecture

React TypeScript frontend sends requests to FastAPI REST APIs. FastAPI validates images, stores data through SQLAlchemy, calls mock ML service, reads knowledge files, and optionally calls OpenRouter from the backend only.
